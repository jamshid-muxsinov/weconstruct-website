services:
  db:
    image: postgres:15
    volumes:
      - db_data:/var/lib/postgresql/data/
    env_file: .env 
    restart: unless-stopped
    environment:
      - PGTZ=Asia/Tashkent

  redis:
    image: redis:7-alpine
    restart: unless-stopped

  site:
    build: .
    # Возвращаем команду к виду с полным путем
    command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000 src.main:site_app
    environment:
      - FORWARDED_ALLOW_IPS=*
      - TZ=Asia/Tashkent
      # Убеждаемся, что PYTHONPATH указывает на корень проекта
      - PYTHONPATH=/app
    volumes:
      - .:/app
      - media_volume:/app/media
    depends_on:
      - db
      - redis
    env_file: .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/ru"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  admin:
    build: .
    # Возвращаем команду к виду с полным путем
    command: gunicorn -w 2 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000 src.main:admin_app
    environment:
      - FORWARDED_ALLOW_IPS=*
      - TZ=Asia/Tashkent
      # Убеждаемся, что PYTHONPATH указывает на корень проекта
      - PYTHONPATH=/app
    volumes:
      - .:/app
      - media_volume:/app/media
    depends_on:
      - db
      - redis
    env_file: .env
    restart: unless-stopped
    healthcheck:
      # Пока оставляем мягкие настройки, чтобы избежать перезапусков
      test: ["CMD", "curl", "-f", "http://localhost:8000/admin/login"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
      
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./src/static:/app/src/static:ro
      - media_volume:/app/media:ro
    depends_on:
      site: 
        condition: service_healthy
      admin: 
        condition: service_healthy
    restart: unless-stopped
    
volumes:
  db_data:
  media_volume: